# Frilo — PRD (Product Requirements Document)

> Telegram-бот для фрилансеров: поиск каналов, генерация продающих сообщений, мониторинг вакансий, автоматическая рассылка.

---

## 1. Описание продукта

**Frilo** — Telegram-бот, который закрывает полный цикл поиска заказов для фрилансера:

1. Находит подходящие каналы и чаты по нише
2. Составляет продающие сообщения для рассылки с помощью LLM
3. Автоматически отправляет их с безопасной частотой
4. Мониторит вакансии в подключённых каналах и помогает откликаться

**Модель:** SaaS — один бот обслуживает всех пользователей. Мультитенантная архитектура с изоляцией данных по `user_id`.

**Два слоя (на продакшене):**
- **Бот-интерфейс** (aiogram 3) — пользователь общается с ботом в Telegram
- **Фоновый userbot-модуль** (Telethon) — рассылает сообщения, парсит каналы от имени пользователя

> **MVP:** userbot отсутствует. Рассылка и парсинг работают только через Bot API (бот должен быть участником каналов/чатов). Userbot добавляется на следующем этапе.

---

## 2. Целевая аудитория

Фрилансеры и самозанятые, ищущие клиентов через Telegram:
- Разработчики, вайбкодеры
- Дизайнеры
- SMM-специалисты
- Копирайтеры
- Специалисты по автоматизации

**Боль:** ручной поиск каналов, ручное написание сообщений, ручная рассылка — рутина, отнимающая часы.

---

## 3. Роли пользователей

| Роль | Описание |
|------|----------|
| **User** | Обычный пользователь бота. Имеет свой профиль, каналы, сообщения, рассылки |
| **Admin** | Администратор системы. Доступ к статистике, управление пользователями, глобальные настройки. Определяется по `telegram_id` в конфиге |

> На этапе монетизации добавятся тарифные планы (Free / Pro / Business), но роль останется одна — `User` с разными лимитами.

---

## 4. Четыре основных модуля

### 4.1. Радар — поиск каналов и чатов

**Задача:** найти места, куда фрилансер может писать о своих услугах или мониторить вакансии.

**Источники поиска:**
- Telegram Search API (поиск по ключевым словам)
- Парсинг каталогов (tgstat.ru и подобные) через aiohttp + BeautifulSoup4
- Перекрёстные упоминания между каналами (в будущем)

**Результат поиска — карточка канала:**
- Название, username, описание
- Количество подписчиков
- Тип: бесплатный / платный
- Тематика (определяется автоматически или вручную)

**Действия пользователя с каналом:**
- Подключить для рассылки
- Подключить для мониторинга вакансий
- Подключить для обоих целей
- Пропустить

**Ограничения MVP:**
- Поиск только через парсинг tgstat.ru и ручной ввод username
- Telegram Search API требует userbot (добавляется позже)

### 4.2. Составить текст — генерация сообщений

**Ключевой модуль.** LLM генерирует продающие сообщения на основе профиля пользователя.

**Зашитая база знаний:**
- Структуры продающего текста (AIDA, PAS, BAB и др.)
- Триггеры внимания
- Антиспам-паттерны (чтобы текст не выглядел как спам)

**Входные данные для генерации:**
- Профиль пользователя (навыки, опыт, ниша, портфолио)
- Целевой канал/чат (тематика, аудитория)
- Тип сообщения (рассылка / отклик на вакансию)

**Возможности:**
- Генерация 2–3 вариантов сообщения
- Персонализация под конкретный канал
- Редактирование и доработка по запросу пользователя
- Сохранение понравившихся сообщений как шаблонов

**Подмодуль: помощь с откликами**
- Пользователь пересылает или вставляет текст вакансии
- Бот генерирует персонализированный отклик
- Может помочь составить краткое резюме под задачу

### 4.3. Найти заказы (Фильтр) — мониторинг вакансий

**Задача:** находить релевантные вакансии в подключённых каналах.

**Двухступенчатая фильтрация:**

| Слой | Технология | Стоимость | Что делает |
|------|-----------|-----------|------------|
| 1 | regex + ключевые слова | Бесплатно | Отсекает ~90% нерелевантных сообщений |
| 2 | LLM-классификация | Токены | Анализирует оставшиеся ~10%, определяет релевантность |

**MVP: работа по запросу.**
Пользователь нажимает «Найти заказы» → бот прогоняет последние сообщения из подключённых каналов → выдаёт до 10 релевантных карточек.

> Фоновый мониторинг (push-уведомления о новых вакансиях) — премиум-фича на будущее.

**Карточка вакансии:**
- Суть задачи (краткое описание от LLM)
- Бюджет (если указан)
- Канал-источник
- Ссылка на оригинальное сообщение
- Дата публикации
- Кнопки: «Подготовить отклик» / «Пропустить» / «В избранное»

### 4.4. Рассылка — автоматическая отправка

**Задача:** автоматически разослать подготовленное сообщение по выбранным каналам.

**Процесс:**
1. Пользователь выбирает сообщение (из сохранённых или генерирует новое)
2. Выбирает каналы для рассылки
3. Подтверждает дисклеймер (при первом использовании)
4. Запускает рассылку
5. Бот отправляет с безопасной частотой и отчитывается

**Ограничения безопасности:**
- Не более 5 сообщений в час (настраиваемый лимит)
- Рандомная задержка между отправками (30–120 сек)
- Уникализация сообщений: LLM делает рерайт для каждого канала (лёгкие вариации)
- Пауза между рассылками в один и тот же канал — минимум 24 часа

**Статусы рассылки:**
- `pending` — ожидает отправки
- `in_progress` — идёт отправка
- `paused` — приостановлена пользователем
- `completed` — все сообщения отправлены
- `failed` — ошибка (канал заблокировал, нет доступа и т.д.)

**MVP-ограничение:** без userbot рассылка возможна только в чаты, где бот является участником. Пользователь добавляет бота в чат вручную.

---

## 5. Экраны и навигация

### 5.1. Главное меню

После `/start` (для существующего пользователя) или после онбординга — отображается главное меню с inline-кнопками:

```
Привет, {имя}! Что делаем?

[Радар]           [Составить текст]
[Найти заказы]    [Рассылка]
[Мой профиль]     [Настройки]
```

### 5.2. Онбординг (первый запуск)

Последовательность шагов, каждый — отдельное сообщение с inline-кнопками или ожиданием текстового ввода.

**Шаг 1: Приветствие**
- Описание бота, что он умеет
- Кнопка «Начать настройку»

**Шаг 2: Специализация**
- «Кто ты?» — мультивыбор inline-кнопками:
  - Разработка
  - Дизайн
  - Маркетинг / SMM
  - Копирайтинг
  - Автоматизация / боты
  - Другое (ввод текстом)
- Кнопка «Далее»

**Шаг 3: Описание услуг**
- «Что ты предлагаешь? Опиши свои услуги, опыт, дай ссылку на портфолио»
- Ввод свободным текстом
- Кнопка «Далее»

**Шаг 4: Параметры поиска заказов**
- «Какие заказы ищешь?»
- Ключевые слова (ввод текстом через запятую)
- Минимальный бюджет (inline-кнопки: «Любой», «от 5000₽», «от 15000₽», «от 50000₽»)
- Формат: разовые / проектные / постоянка (мультивыбор)
- Кнопка «Далее»

**Шаг 5: Подключение каналов**
- Бот предлагает стартовый набор каналов по выбранной нише
- Для каждого канала — кнопки: «Рассылка» / «Вакансии» / «Оба» / «Пропустить»
- Кнопка «Готово»

**Шаг 6: Дисклеймер**
- Текст о рисках блокировки при рассылке
- Кнопки: «Принимаю риски» / «Не буду использовать рассылку»

**Шаг 7: Готово**
- «Профиль настроен! Вот что можно сделать...»
- Переход в главное меню

### 5.3. Радар

**Экран: список подключённых каналов**
```
Подключённые каналы (12):

📡 @frontend_jobs — вакансии
📡 @webdev_chat — рассылка + вакансии
📡 @design_ru — рассылка
...

[Найти новые каналы]  [Управление каналами]
[← Меню]
```

**Экран: поиск каналов**
```
Введи ключевое слово или нишу для поиска:
```

После ввода — список найденных каналов (карточки).

**Карточка канала:**
```
@channel_name — «Название канала»
Подписчиков: 12 500
Тип: бесплатный
Описание: ...

[Рассылка] [Вакансии] [Оба] [Пропустить]
```

**Экран: управление каналами**
- Список с возможностью изменить назначение (рассылка/вакансии) или отключить канал

### 5.4. Составить текст

**Экран: выбор действия**
```
Что составляем?

[Сообщение для рассылки]
[Отклик на вакансию]
[Мои шаблоны]
[← Меню]
```

**Экран: генерация сообщения для рассылки**
```
Для каких каналов генерируем?

[Все для рассылки]
[Выбрать конкретные]
```

После выбора — бот генерирует 2–3 варианта:
```
Вариант 1:
«Текст сообщения...»

Вариант 2:
«Текст сообщения...»

[Выбрать 1] [Выбрать 2]
[Переделать] [Редактировать вручную]
```

После выбора:
```
Сообщение сохранено!

[Запустить рассылку] [В шаблоны] [← Меню]
```

**Экран: отклик на вакансию**
```
Отправь мне текст вакансии или перешли сообщение:
```

После получения текста — бот генерирует отклик:
```
Вот отклик:
«Текст отклика...»

[Скопировать] [Переделать] [Редактировать]
```

**Экран: мои шаблоны**
- Список сохранённых сообщений
- Для каждого: «Использовать» / «Редактировать» / «Удалить»

### 5.5. Найти заказы

**Экран: поиск**
```
Поиск вакансий по вашим параметрам.
Подключено каналов для мониторинга: 8

[Найти заказы]
[Изменить параметры поиска]
[← Меню]
```

**Экран: результаты**
```
Найдено 7 вакансий:

1/7
Нужен Telegram-бот для интернет-магазина
Бюджет: 30 000₽
Канал: @freelance_orders
Опубликовано: 2 часа назад
[Оригинал]

[Подготовить отклик] [В избранное] [Пропустить →]
```

Навигация стрелками «←» / «→» между карточками.

**Экран: избранное**
- Список сохранённых вакансий
- Для каждой: «Подготовить отклик» / «Убрать из избранного»

### 5.6. Рассылка

**Экран: панель рассылки**
```
Рассылка

Активная: «Разработка ботов...» — 5/12 каналов ✅
Следующая отправка через: 8 мин

[Приостановить] [Остановить]
[Новая рассылка] [История рассылок]
[← Меню]
```

**Экран: новая рассылка**
```
1. Выбери сообщение:
[Последнее сгенерированное]
[Из шаблонов]
[Написать новое]
```
→
```
2. Выбери каналы:
[Все для рассылки (12)]
[Выбрать вручную]
```
→
```
3. Подтверждение:
Сообщение: «Текст...»
Каналы: 12
Примерное время: ~2.5 часа

[Запустить] [Отмена]
```

**Экран: история рассылок**
```
Последние рассылки:

16.02 — «Разработка ботов...» — 12/12 ✅
14.02 — «Дизайн лендингов...» — 8/10 ⚠️ (2 ошибки)
10.02 — «Автоматизация...» — 5/5 ✅

[Подробнее] для каждой
```

### 5.7. Мой профиль

```
Твой профиль:

Специализации: Разработка, Автоматизация
Услуги: «Разрабатываю Telegram-ботов...»
Портфолио: github.com/...
Ключевые слова: бот, telegram, python, автоматизация
Мин. бюджет: от 15 000₽
Формат: проекты, постоянка

[Редактировать специализации]
[Редактировать описание]
[Редактировать параметры поиска]
[← Меню]
```

### 5.8. Настройки

```
Настройки:

Рассылка:
  Лимит: 5 сообщений/час
  Тихие часы: 23:00 — 08:00
  Задержка между отправками: 30–120 сек

Вакансии:
  Уведомления: выключены (MVP)

[Изменить лимит рассылки]
[Тихие часы]
[← Меню]
```

---

## 6. База данных

### 6.1. ER-диаграмма (текстовая)

```
users ──1:N── user_channels
users ──1:N── messages
users ──1:N── broadcasts
users ──1:N── vacancies_saved
users ──1:N── search_profiles

channels ──1:N── user_channels
channels ──1:N── channel_messages

broadcasts ──1:N── broadcast_items
broadcast_items ──N:1── user_channels
broadcast_items ──N:1── messages

messages ──1:N── broadcast_items
```

### 6.2. Таблицы

#### `users`

Основная таблица пользователей.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | Внутренний ID |
| `telegram_id` | `BIGINT` UNIQUE NOT NULL | Telegram user ID |
| `username` | `VARCHAR(255)` | Telegram username |
| `first_name` | `VARCHAR(255)` | Имя |
| `specializations` | `TEXT[]` | Массив специализаций |
| `services_description` | `TEXT` | Описание услуг |
| `portfolio_url` | `VARCHAR(500)` | Ссылка на портфолио |
| `onboarding_completed` | `BOOLEAN` DEFAULT false | Пройден ли онбординг |
| `disclaimer_accepted` | `BOOLEAN` DEFAULT false | Принят ли дисклеймер рассылки |
| `is_admin` | `BOOLEAN` DEFAULT false | Флаг администратора |
| `subscription_tier` | `VARCHAR(50)` DEFAULT 'free' | Тарифный план (заготовка) |
| `created_at` | `TIMESTAMPTZ` | Дата регистрации |
| `updated_at` | `TIMESTAMPTZ` | Дата обновления |

#### `search_profiles`

Параметры поиска вакансий. Отдельная таблица — у пользователя может быть несколько профилей поиска в будущем.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users | |
| `keywords` | `TEXT[]` | Ключевые слова для поиска |
| `min_budget` | `INTEGER` | Минимальный бюджет (руб.) |
| `work_format` | `TEXT[]` | Формат: `['one_time', 'project', 'permanent']` |
| `is_active` | `BOOLEAN` DEFAULT true | |
| `created_at` | `TIMESTAMPTZ` | |

#### `channels`

Глобальный справочник каналов. Один канал — одна запись, даже если его подключили 100 пользователей.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `telegram_id` | `BIGINT` UNIQUE | Telegram chat ID |
| `username` | `VARCHAR(255)` | @username канала |
| `title` | `VARCHAR(500)` | Название |
| `description` | `TEXT` | Описание |
| `subscribers_count` | `INTEGER` | Подписчики (обновляется периодически) |
| `is_paid` | `BOOLEAN` DEFAULT false | Платный канал |
| `category` | `VARCHAR(100)` | Тематика |
| `source` | `VARCHAR(50)` | Откуда найден: `tgstat`, `manual`, `search` |
| `last_parsed_at` | `TIMESTAMPTZ` | Когда последний раз парсили сообщения |
| `created_at` | `TIMESTAMPTZ` | |

#### `user_channels`

Связь пользователя с каналами. Определяет, для чего пользователь подключил канал.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users | |
| `channel_id` | `UUID` FK → channels | |
| `purpose` | `VARCHAR(20)` NOT NULL | `broadcast` / `monitor` / `both` |
| `is_active` | `BOOLEAN` DEFAULT true | |
| `created_at` | `TIMESTAMPTZ` | |

UNIQUE constraint на `(user_id, channel_id)`.

#### `messages`

Сообщения, составленные пользователем (шаблоны для рассылки, отклики).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users | |
| `type` | `VARCHAR(20)` | `broadcast` / `response` |
| `content` | `TEXT` NOT NULL | Текст сообщения |
| `is_template` | `BOOLEAN` DEFAULT false | Сохранён как шаблон |
| `metadata` | `JSONB` | Доп. данные (канал, для которого создано и т.д.) |
| `created_at` | `TIMESTAMPTZ` | |

#### `broadcasts`

Рассылки. Одна рассылка = одно сообщение → N каналов.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users | |
| `message_id` | `UUID` FK → messages | Какое сообщение рассылается |
| `status` | `VARCHAR(20)` | `pending` / `in_progress` / `paused` / `completed` / `failed` |
| `total_channels` | `INTEGER` | Всего каналов |
| `sent_count` | `INTEGER` DEFAULT 0 | Отправлено |
| `error_count` | `INTEGER` DEFAULT 0 | Ошибок |
| `started_at` | `TIMESTAMPTZ` | |
| `completed_at` | `TIMESTAMPTZ` | |
| `created_at` | `TIMESTAMPTZ` | |

#### `broadcast_items`

Отдельные отправки внутри рассылки (одно сообщение в один канал).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `broadcast_id` | `UUID` FK → broadcasts | |
| `user_channel_id` | `UUID` FK → user_channels | Куда отправлять |
| `status` | `VARCHAR(20)` | `pending` / `sent` / `failed` / `skipped` |
| `sent_at` | `TIMESTAMPTZ` | Когда отправлено |
| `error_message` | `TEXT` | Текст ошибки |
| `unique_content` | `TEXT` | Уникализированный текст (рерайт) |
| `created_at` | `TIMESTAMPTZ` | |

#### `channel_messages`

Сообщения, спарсенные из каналов. Используются для поиска вакансий.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `channel_id` | `UUID` FK → channels | |
| `telegram_message_id` | `BIGINT` | ID сообщения в Telegram |
| `text` | `TEXT` | Текст сообщения |
| `date` | `TIMESTAMPTZ` | Дата публикации в канале |
| `is_vacancy` | `BOOLEAN` | Прошло ли фильтр как вакансия (NULL = не проверено) |
| `vacancy_data` | `JSONB` | Извлечённые данные: бюджет, навыки, описание |
| `created_at` | `TIMESTAMPTZ` | |

UNIQUE constraint на `(channel_id, telegram_message_id)`.

#### `saved_vacancies`

Вакансии, сохранённые пользователем (избранное).

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users | |
| `channel_message_id` | `UUID` FK → channel_messages | |
| `status` | `VARCHAR(20)` | `saved` / `responded` / `dismissed` |
| `response_text` | `TEXT` | Текст отклика (если составлен) |
| `created_at` | `TIMESTAMPTZ` | |

#### `settings`

Пользовательские настройки.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `UUID` PK | |
| `user_id` | `UUID` FK → users UNIQUE | |
| `broadcast_limit_per_hour` | `INTEGER` DEFAULT 5 | Лимит сообщений в час |
| `quiet_hours_start` | `TIME` DEFAULT '23:00' | Начало тихих часов |
| `quiet_hours_end` | `TIME` DEFAULT '08:00' | Конец тихих часов |
| `min_delay_seconds` | `INTEGER` DEFAULT 30 | Мин. задержка между отправками |
| `max_delay_seconds` | `INTEGER` DEFAULT 120 | Макс. задержка |
| `updated_at` | `TIMESTAMPTZ` | |

### 6.3. Индексы

```sql
-- Быстрый поиск пользователя по telegram_id
CREATE INDEX idx_users_telegram_id ON users(telegram_id);

-- Каналы пользователя
CREATE INDEX idx_user_channels_user_id ON user_channels(user_id);
CREATE INDEX idx_user_channels_purpose ON user_channels(user_id, purpose);

-- Рассылки пользователя
CREATE INDEX idx_broadcasts_user_id ON broadcasts(user_id);
CREATE INDEX idx_broadcasts_status ON broadcasts(status);

-- Элементы рассылки
CREATE INDEX idx_broadcast_items_broadcast_id ON broadcast_items(broadcast_id);
CREATE INDEX idx_broadcast_items_status ON broadcast_items(status);

-- Сообщения каналов для поиска вакансий
CREATE INDEX idx_channel_messages_channel_id ON channel_messages(channel_id);
CREATE INDEX idx_channel_messages_date ON channel_messages(date DESC);
CREATE INDEX idx_channel_messages_vacancy ON channel_messages(is_vacancy) WHERE is_vacancy = true;

-- Шаблоны пользователя
CREATE INDEX idx_messages_user_templates ON messages(user_id) WHERE is_template = true;
```

### 6.4. Заготовка под монетизацию

В таблице `users` уже есть поле `subscription_tier`. Когда придёт время, добавятся таблицы:

- **`subscription_plans`** — тарифы (free, pro, business), лимиты каждого
- **`payments`** — история платежей
- **`usage_stats`** — счётчики использования (генераций LLM, рассылок, поисков) для контроля лимитов

Лимиты будут контролироваться на уровне приложения по `subscription_tier`.

---

## 7. Технический стек

### MVP (разработка)

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.11+ |
| Telegram Bot | aiogram 3 |
| Userbot (позже) | Telethon |
| LLM | Определим позже |
| БД | Supabase Cloud (PostgreSQL, 500 МБ, бесплатный тариф) |
| Python-клиент БД | supabase-py |
| Планировщик задач | APScheduler (asyncio) |
| HTTP-клиент | aiohttp |
| Парсинг HTML | BeautifulSoup4 |
| Фильтрация | re (стандартная библиотека) |
| Хостинг | Локальная машина / Render.com (бесплатный тариф) |

### Продакшен (есть пользователи)

| Компонент | Технология |
|-----------|-----------|
| БД | PostgreSQL на российском VPS |
| Python-клиент БД | asyncpg или SQLAlchemy + asyncio |
| Управление БД | pgAdmin / DBeaver |
| Планировщик | APScheduler → Celery + Redis (при необходимости) |
| Хостинг | VPS Timeweb Cloud / Selectel, Ubuntu 24, 2 ядра, 4 ГБ RAM |

---

## 8. Структура проекта

```
frilobot/
├── bot/                          # Telegram Bot (aiogram 3)
│   ├── __init__.py
│   ├── __main__.py               # Точка входа: запуск бота
│   ├── config.py                 # Настройки из env-переменных
│   │
│   ├── handlers/                 # Обработчики сообщений (по модулям)
│   │   ├── __init__.py
│   │   ├── start.py              # /start, онбординг
│   │   ├── menu.py               # Главное меню, навигация
│   │   ├── radar.py              # Модуль «Радар»
│   │   ├── compose.py            # Модуль «Составить текст»
│   │   ├── vacancies.py          # Модуль «Найти заказы»
│   │   ├── broadcast.py          # Модуль «Рассылка»
│   │   ├── profile.py            # Модуль «Мой профиль»
│   │   └── settings.py           # Модуль «Настройки»
│   │
│   ├── keyboards/                # Inline- и reply-клавиатуры
│   │   ├── __init__.py
│   │   ├── menu.py               # Главное меню
│   │   ├── onboarding.py         # Кнопки онбординга
│   │   ├── radar.py              # Кнопки радара
│   │   ├── compose.py            # Кнопки генерации текста
│   │   ├── vacancies.py          # Кнопки вакансий
│   │   └── broadcast.py          # Кнопки рассылки
│   │
│   ├── states/                   # FSM-состояния (aiogram)
│   │   ├── __init__.py
│   │   ├── onboarding.py         # Состояния онбординга
│   │   ├── compose.py            # Состояния генерации текста
│   │   └── broadcast.py          # Состояния настройки рассылки
│   │
│   ├── middlewares/               # Мидлвари aiogram
│   │   ├── __init__.py
│   │   ├── auth.py               # Проверка пользователя, создание если нет
│   │   └── throttling.py         # Антифлуд
│   │
│   ├── filters/                  # Кастомные фильтры aiogram
│   │   ├── __init__.py
│   │   └── admin.py              # Фильтр «только админ»
│   │
│   └── callbacks/                # Callback data factories
│       ├── __init__.py
│       └── pagination.py         # Пагинация карточек
│
├── services/                     # Бизнес-логика (не зависит от aiogram)
│   ├── __init__.py
│   ├── radar.py                  # Поиск каналов: tgstat, search
│   ├── composer.py               # Генерация текстов через LLM
│   ├── vacancy_filter.py         # Двухступенчатая фильтрация вакансий
│   ├── broadcaster.py            # Логика рассылки: очередь, задержки, уникализация
│   └── channel_parser.py         # Парсинг сообщений из каналов
│
├── llm/                          # Интеграция с LLM
│   ├── __init__.py
│   ├── client.py                 # Клиент LLM (абстракция, чтобы менять провайдера)
│   └── prompts/                  # Промпты
│       ├── __init__.py
│       ├── broadcast_message.py  # Промпт генерации рассылочного сообщения
│       ├── vacancy_response.py   # Промпт генерации отклика
│       ├── vacancy_classify.py   # Промпт классификации вакансии
│       └── rewrite.py            # Промпт уникализации текста
│
├── db/                           # Работа с БД
│   ├── __init__.py
│   ├── connection.py             # Подключение (supabase-py → asyncpg)
│   └── repositories/             # Репозитории (паттерн Repository)
│       ├── __init__.py
│       ├── users.py
│       ├── channels.py
│       ├── messages.py
│       ├── broadcasts.py
│       ├── vacancies.py
│       └── settings.py
│
├── scheduler/                    # Планировщик задач (APScheduler)
│   ├── __init__.py
│   └── jobs.py                   # Задачи: рассылка, парсинг каналов
│
├── parsers/                      # Парсеры внешних источников
│   ├── __init__.py
│   ├── tgstat.py                 # Парсер tgstat.ru
│   └── keyword_filter.py        # Regex-фильтр (первый слой)
│
├── userbot/                      # Telethon userbot (пустой на MVP)
│   ├── __init__.py
│   ├── client.py                 # Клиент Telethon
│   └── handlers.py               # Обработчики событий userbot
│
├── utils/                        # Утилиты
│   ├── __init__.py
│   └── text.py                   # Хелперы для текста (обрезка, экранирование)
│
├── migrations/                   # SQL-миграции
│   └── 001_initial.sql
│
├── .env.example                  # Пример переменных окружения
├── .gitignore
├── requirements.txt
├── pyproject.toml
├── Dockerfile                    # Для деплоя на VPS
└── README.md
```

### Принципы структуры

**Разделение слоёв:**
- `bot/` — только Telegram-специфичный код (хендлеры, клавиатуры, FSM). Ничего про БД или бизнес-логику.
- `services/` — бизнес-логика. Не знает про aiogram. Можно вызвать из бота, из CLI, из тестов.
- `db/` — доступ к данным. Паттерн Repository изолирует SQL-запросы. При переезде с supabase-py на asyncpg меняется только `connection.py` и внутренности репозиториев, интерфейс остаётся.
- `llm/` — абстракция над LLM-провайдером. Промпты хранятся отдельно, клиент — отдельно. Смена провайдера = замена `client.py`.

**Масштабирование:**
- Каждый модуль бота (радар, рассылка, вакансии) — отдельный файл в `handlers/`, `keyboards/`, `states/`. Добавление нового модуля не трогает существующие.
- `userbot/` пока пустой — заполнится при добавлении Telethon, не ломая текущую структуру.
- `scheduler/` легко расширяется новыми задачами. При переезде на Celery — меняется только этот модуль.

**Поток данных:**

```
Telegram → bot/handlers → services → db/repositories → Supabase/PostgreSQL
                              ↓
                           llm/client → LLM API
                              ↓
                          parsers/ → tgstat.ru / regex
```

---

## 9. Переменные окружения

```env
# Telegram
BOT_TOKEN=               # Токен бота от @BotFather
ADMIN_IDS=123456,789012  # Telegram ID админов (через запятую)

# Supabase (MVP)
SUPABASE_URL=             # URL проекта Supabase
SUPABASE_KEY=             # anon key или service_role key

# PostgreSQL (продакшен, вместо Supabase)
# DATABASE_URL=postgresql://user:pass@host:5432/frilo

# LLM
LLM_API_KEY=              # API-ключ LLM-провайдера
LLM_MODEL=                # Название модели

# Рассылка (дефолты)
DEFAULT_BROADCAST_LIMIT=5
DEFAULT_MIN_DELAY=30
DEFAULT_MAX_DELAY=120
```

---

## 10. Этапы разработки

### Этап 1 — Фундамент
- Структура проекта, конфиг, подключение к Supabase
- Таблицы БД (миграции)
- `/start`, онбординг, сохранение профиля
- Главное меню

### Этап 2 — Составить текст
- Интеграция LLM
- Генерация рассылочных сообщений
- Генерация откликов на вакансии
- Сохранение шаблонов

### Этап 3 — Радар
- Парсер tgstat.ru
- Ручное добавление каналов по username
- Управление подключёнными каналами

### Этап 4 — Найти заказы
- Парсинг сообщений из подключённых каналов
- Regex-фильтр (первый слой)
- LLM-классификация (второй слой)
- Карточки вакансий, избранное

### Этап 5 — Рассылка
- Выбор сообщения и каналов
- APScheduler: очередь с задержками
- Уникализация через LLM
- Статусы, отчёты, история
- Дисклеймер

### Этап 6 — Userbot (Telethon)
- Авторизация пользователя
- Рассылка от имени пользователя
- Поиск каналов через Telegram Search API
- Парсинг каналов, в которых бот не участник

### Этап 7 — Монетизация
- Тарифные планы, лимиты
- Интеграция платежей
- Фоновый мониторинг вакансий (премиум)

---

## 11. Риски и ограничения

| Риск | Митигация |
|------|----------|
| Блокировка аккаунта при рассылке | Лимиты, задержки, уникализация, дисклеймер |
| Блокировка бота в каналах | Ротация текстов, уважение правил каналов |
| Расход токенов LLM | Двухступенчатая фильтрация, кеширование, работа по запросу |
| Лимиты Supabase (500 МБ) | Чистка старых `channel_messages`, переезд на VPS при росте |
| Telegram API rate limits | Exponential backoff, очередь с контролем частоты |
| tgstat.ru изменит вёрстку | Парсер легко заменяемый, модульная архитектура |

---

## 12. Что НЕ входит в MVP

- Userbot (Telethon) — этап 6
- Фоновый мониторинг вакансий (push) — этап 7
- Монетизация — этап 7
- Telegram Search API (требует userbot)
- Помощь с тестовыми заданиями (сложная фича, низкий приоритет)
- Аналитика (дашборд, статистика конверсий)
- Мультиязычность
